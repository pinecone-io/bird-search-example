PINECONE_API_KEY=

# Embedding provider: "local" (default, SigLIP, no key needed) or "gemini"
# (Gemini Embedding 2, requires GOOGLE_API_KEY). Leave unset for local.
EMBED_PROVIDER=local
GOOGLE_API_KEY=
